import { Base64imageDirective } from './base64image.directive';

describe('Base64imageDirective', () => {
  it('should create an instance', () => {
    const directive = new Base64imageDirective();
    expect(directive).toBeTruthy();
  });
});
